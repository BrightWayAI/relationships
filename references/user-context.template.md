# relationships user context (TEMPLATE)

_Generated by `/setup-relationships`. See that command's spec for the peer-import logic that fills this file._

_This file holds ONLY relationships-owned configuration. Identity, voice, ICP, CRM properties, Apollo config, and referral cooling rules are read live at runtime from peer-plugin files (`identity.md`, `voice.md`, `lead-engine.user-context.md`, `core-ops.user-context.md`, `referral-engine.user-context.md`). The peer-import step in setup imports them ONE TIME into the dictionary for confirmation, but they are not re-stored here._

_Last updated: [filled by setup]_

---

## Identity (minimal — for template variable filling)

- **Name:** [from `identity.md`]
- **First-name form:** [used in templates, e.g., "Zach" vs "Zachary"]
- **Company:** [from `identity.md`]
- **One-liner:** [one-sentence description; from `identity.md` or `bizdev-outreach.user-context.md`]

## Companion plugins (runtime-detected)

- **cortex:** [Y/N — `<config-root>/memory/` exists]
- **lead-engine:** [Y/N — `lead-engine.user-context.md` exists; if Y, ICP + Apollo + signals come from here]
- **core-ops:** [Y/N — `core-ops.user-context.md` exists; if Y, CRM config comes from here]
- **referral-engine:** [Y/N — `referral-engine.user-context.md` exists; if Y, cooling rules come from here]
- **daily-brief:** [Y/N]
- **writing-style:** [Y/N — if Y, voice rules come from here in addition to `voice.md`]

## Tiers (configurable, defaults shown)

- **Inner Core:** size [10–15] · cadence [14] days
- **Strategic:** size [30–50] · cadence [30] days
- **Operational:** size [100–200] · cadence [90] days
- **Dormant:** trigger-only

## Close personal track

- **Enabled:** [Y/N — default N]
- **Size:** [if Y, e.g., 10]
- **Cadence:** [if Y, days; default 30]

## Buckets

- **New Business Development:** weight [1.0]
- **Relationship Building:** weight [1.0]
- **Network Expansion:** weight [1.0]
- **Hide empty buckets:** [Y/N — default N]

## Network-expansion voices

If the user posts in more than one voice (e.g., personal brand + business brand), name each and what surface it's used for. Each voice block is the surface routing target for content-posting templates.

```
### Voice: [name — e.g., "personal" or "brightway"]
- **Surface:** [where this voice is used — LinkedIn personal / business page / etc.]
- **Topic stream:** [practical operational AI / targeted education / etc.]
```

If only one voice is used (the default), just note: `Single voice — uses primary from voice.md`.

## Time budget

- **Typical daily availability:** [5 / 15 / 30 / 60+ minutes — default 30]
- **Highly variable:** [Y/N — if Y, brief asks at top of each run; default N]

## Scoring overrides (optional — advanced)

Defaults in `references/scoring.md`. Override per bucket only if needed.

```
### new_biz bucket
- w_decay: 0.20
- w_icp: 0.40
```

If not specified, all buckets use defaults.

---

## Standalone-install fallbacks

This section ONLY populates when a peer file is missing at setup time. The plugin uses these fields directly when the corresponding peer is absent. If the peer is later installed, re-running `/setup-relationships` re-imports and this section gets cleared.

### Identity fallback (only if `identity.md` missing)
[full identity block]

### Voice fallback (only if `voice.md` missing)
[voice descriptors, sign-off, banned phrases]

### ICP fallback (only if `lead-engine.user-context.md` missing)
[primary + secondary ICP definitions, out-of-ICP]

### CRM fallback (only if `core-ops.user-context.md` missing)
[CRM tool, owner ID, pipeline stages, custom property names]

### Apollo fallback (only if `lead-engine.user-context.md` missing)
[Apollo enabled, daily DM budget, weekly cap, signal preferences]

### Referral cooling fallback (only if `referral-engine.user-context.md` missing)
[connector taxonomy, quiet threshold, trigger patterns, ask cadence cap]

---

## Provenance (auto-generated by setup)

Track which fields came from which peer file. Helps debugging when something feels off.

```
- Identity: <config-root>/identity.md (2026-05-11)
- Primary voice: <config-root>/voice.md (2026-05-11)
- Primary ICP: <config-root>/plugins/lead-engine.user-context.md (2026-05-11)
- CRM: <config-root>/plugins/core-ops.user-context.md (2026-05-11)
- Apollo: <config-root>/plugins/lead-engine.user-context.md (2026-05-11)
- Cooling: <config-root>/plugins/referral-engine.user-context.md (2026-05-11)
```
